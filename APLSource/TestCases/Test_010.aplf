 R←Test_010(stopFlag batchFlag);⎕TRAP;path;A
⍝ Exercise `GetUserCommandFolder`
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 A←##.APLTreeUtils

 path←##.Maker.GetUserCommandFolder'foogoozoo.doesnotexist'   ⍝ Should not exist!
 →T.PassesIf'documents\myucmds'{⍺≡(-⍴⍺)↑⍵}A.Lowercase path

 :If 'kai'≡A.Lowercase ⎕AN
     path←##.Maker.GetUserCommandFolder'fire_uc.dyalog'       ⍝ Exists on my PCs, therefore the check for ⎕AN
     →T.PassesIf∨/'c:\users\kai\documents\myucmds'⍷A.Lowercase path
 :EndIf

 R←T._OK
⍝Done
