 {r}←EstablishRefs parms;def
⍝ Takes `parms` (publish.config) and processes the `refs` variables.
⍝ Returns the number of references established.
 :If 0<r←≢parms.refs
     ⍎¨parms.refs[;3]
 :EndIf
⍝Done
